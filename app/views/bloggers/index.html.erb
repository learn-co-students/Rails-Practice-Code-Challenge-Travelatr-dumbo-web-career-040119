<%= render 'header' %>
<h1>All bloggers:</h1>

<ul>
  <% @bloggers.all.each do |blogger| %>
  <li><%= link_to blogger.name, blogger_path(blogger) %></li>
  <% end %>
</ul>
<br>

<%= link_to "Become a blogger!", new_blogger_path %>
